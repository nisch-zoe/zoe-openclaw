# Heartbeat

1. Skim `PULSE.md` — is anything stale or does it need a follow-up nudge?
2. If `Mode` is `resting` or `Quiet Until` is still active, reply `HEARTBEAT_OK`.
3. If "Follow Up" items are more than 3 days old, consider a brief message.
4. If nothing needs attention: `HEARTBEAT_OK`.
