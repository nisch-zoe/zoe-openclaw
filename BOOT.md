# BOOT.md - Gateway Startup

1. Read `PULSE.md` for current state.
2. Run `node scripts/db.js summary --json` to verify the DB is healthy.
3. Check `memory/active-tasks.md` for incomplete work.
4. If all clear: `HEARTBEAT_OK`
5. Otherwise: report what needs attention.
