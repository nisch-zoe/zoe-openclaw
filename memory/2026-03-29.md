# Daily Log: 2026-03-29

## OpenClaw
- Added a local workout tracking system with SQLite session history, flexible exercise payloads, and draft-based follow-up state for Telegram capture.
- Added `workspace/scripts/workouts.js` plus the `workout-capture`, `workout-follow-up`, and `workout-review` OpenClaw skills.
- Moved the embedded GitHub repos out to `~/Repos/` and left symlinks in `workspace/` so the OpenClaw backup repo can stay clean.
- Added `scripts/backup-openclaw.sh` plus `scripts/install-openclaw-backup-cron.sh` for unattended workspace backups; the cron job now commits changes automatically and pushes when a remote is configured.
- Added `scripts/backup-openclaw-databases.js` and wired it into `scripts/backup-openclaw.sh` so `data/openclaw.db` now gets snapshotted into `workspace/backups/databases/` before the Git backup runs.
- Verified the old Crabby India market close cron job (`6492b53d-b9aa-49be-be49-c8085b46e06d`) is no longer present in the live scheduler; `openclaw cron list --all` shows only the 4 main Zoe jobs, and disabling that legacy ID now returns unknown job id.
